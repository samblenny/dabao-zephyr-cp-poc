[package]
name = "dabao-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
panic = "abort"       # Disable panic unwind

[profile.release]
panic = "abort"       # Disable panic unwind
